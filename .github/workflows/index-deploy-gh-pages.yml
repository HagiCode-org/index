name: Index Deploy gh-pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: index-deploy-gh-pages
  cancel-in-progress: false

permissions:
  contents: read

env:
  NODE_VERSION: "22.12.0"
  INDEX_GH_PAGES_PAYLOAD_DIR: .deploy/gh-pages

jobs:
  build:
    name: Build validated index snapshot
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build and verify index
        run: npm run build

      - name: Assemble gh-pages publication payload
        run: |
          rm -rf "${INDEX_GH_PAGES_PAYLOAD_DIR}"
          mkdir -p "${INDEX_GH_PAGES_PAYLOAD_DIR}/dist"
          cp .github/gh-pages/esa.jsonc "${INDEX_GH_PAGES_PAYLOAD_DIR}/esa.jsonc"
          cp -R dist/. "${INDEX_GH_PAGES_PAYLOAD_DIR}/dist/"

      - name: Upload validated gh-pages payload
        uses: actions/upload-artifact@v4
        with:
          name: index-gh-pages-payload
          path: ${{ env.INDEX_GH_PAGES_PAYLOAD_DIR }}
          if-no-files-found: error

  deploy:
    name: Publish validated snapshot to gh-pages
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    environment:
      name: index-production
      url: https://index.hagicode.com
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download validated gh-pages payload
        uses: actions/download-artifact@v4
        with:
          name: index-gh-pages-payload
          path: .deploy/gh-pages

      - name: Publish validated gh-pages payload
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./.deploy/gh-pages
          force_orphan: true
          enable_jekyll: false

      - name: Summarize deployment output
        run: |
          echo "Published the validated gh-pages payload with ./esa.jsonc and ./dist/." >> "$GITHUB_STEP_SUMMARY"
          echo "Index production now consumes the CI-owned gh-pages branch payload." >> "$GITHUB_STEP_SUMMARY"
          echo "GitHub environment: index-production" >> "$GITHUB_STEP_SUMMARY"
          echo "Expected public URL: https://index.hagicode.com" >> "$GITHUB_STEP_SUMMARY"
